This repo uses `AGENTS.md` (root) for agent onboarding/context.

Open `AGENTS.md` for:
- critical dataset keys (`instruction_key=task` for LIBERO)
- branch guidance (`phaseC-flow-head` vs `master`)
- experiment matrix + eval checklist

